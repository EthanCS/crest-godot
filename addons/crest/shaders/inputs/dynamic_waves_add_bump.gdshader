shader_type spatial;
render_mode unshaded, depth_draw_never, cull_disabled;

// Crest/Inputs/Dynamic Waves/Add Bump
uniform float _Amplitude = 1.0;
uniform float _Radius = 3.0;

void fragment() { discard; }
